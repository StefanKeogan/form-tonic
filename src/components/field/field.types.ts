export interface IRenderFieldProps {
  field: string
  value: string | number
  values?: Array<string | number>
}

export interface IFieldProps {
  value?: string | number
  values?: Array<string | number>
}